#include <stdio.h>
#include <math.h>

int main() {
	int x1;
	double eps;
	double rez = scanf("%d%lf\n", &x1, &eps);
	double ch = 1;
	double sum = 0;
	int k = 0;
	double y1;
	
	if (rez == 2) {
		double y2 = cos(x1);
		while (ch > eps) {
			k++;
			int i, F;
			while (i <= k) {
				F = F * i;
				i++;
			}
			for (int s = k; s <= k; k *= 2) {

			}
			sum += ((-1)**k) * ((x ** 2)*k)/(2*k)!)
		}
		
	}
	else {
		printf("WRONG DATA!");
	}

}
